# Copiaza ca .env.production pe server si completeaza cu valori reale.
# Genereaza secrete random cu:
#   openssl rand -base64 32
#   openssl rand -base64 48

POSTGRES_USER=unplgd
POSTGRES_PASSWORD=REPLACE_ME_STRONG_RANDOM
POSTGRES_DB=unplgd

JWT_SECRET=REPLACE_ME_STRONG_RANDOM
